!!!
%html
  %head
    %meta{charset: "utf-8"}/
    %meta{name: "viewpot", content: "width=device-width, inital-scale=1.0"}/
    %title
      = content_for?(:title) ? yield(:title) : "MEZZOE - COMING SOON"
    = favicon_link_tag('mfav.ico', type: 'image/x-icon')
    = stylesheet_link_tag 'http://fonts.googleapis.com/css?family=Raleway'
    = stylesheet_link_tag "application"
    = csrf_meta_tags

  %body
    - if content_for?(:header)
      %header= yield :header
    = yield
    = javascript_include_tag "application"

